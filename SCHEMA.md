---
schema: "0.1"
coverage: listed
---

# SCHEMA — README context engine

> Consolidated-context submodule of the `bamr87/bamr87` monorepo: crawlers,
> distillers, and an MCP server that turn the fleet's repositories into a
> queryable, continuously regenerated README pyramid.

## Conventions

- Structural facts live in the registry (`_data/projects.yml`); generated
  surfaces (`context/`, the README `AUTO:projects` span, `repos.txt`,
  `docs/index.md`, build output) are regenerated, never hand-edited.
- The context pyramid distills upward: `docs/` corpus (L3) → `context/facts/`
  (L2) → `context/cards/` (L1) → `context/README.md` apex (L0). Rebuild with
  `python3 -m scripts.context_engine build`.
- `docs/*` mirrors independent repos: the pyramid stops at their boundary
  (`terminal`); each fleet project carries its own pyramid in its own repo.
- Validate with `python3 scripts/schema_lint.py check .` — wired into the
  docs quality gate.

## Structure

| entry | kind | purpose | rules |
|---|---|---|---|
| `.github/` | dir | Actions control plane: aggregation cron, quality gate, pages deploy, standard CI caller | terminal |
| `_data/` | dir | Registries — projects.yml is the source of truth for the fleet | required |
| `context/` | dir | The context pyramid: facts, cards, apex, query index (rebuilt by the engine) | generated |
| `docs/` | dir | L3 corpus: markdown aggregated from fleet repos (build artifact of stages 1–3) | generated |
| `hooks.d/` | dir | Lifecycle hooks the engine runs at each build stage | required |
| `mcp/` | dir | MCP server exposing the pyramid to AI clients | required |
| `scripts/` | dir | Pipeline stages, context engine, gates, and utilities | required |
| `tests/` | dir | Unit + integration harness (`python tests/test_runner.py`) | required |
| `.editorconfig` | file | Editor defaults (standardization baseline) | |
| `.env.example` | file | Environment template: AI provider keys, Wiki.js settings | |
| `.gitignore` | file | Ignore rules for temp/, raw_docs/, site/, venvs | |
| `.mcp.json` | file | MCP server registration for Claude Code and other clients | required |
| `CLAUDE.md` | file | Claude Code guidance, including the SCHEMA protocol | required |
| `LICENSE` | file | MIT license | |
| `MKDOCS.md` | file | Site build documentation | |
| `PRD.md` | file | Product requirements — context engine spec (v2) | |
| `README.md` | file | Repo README — AUTO:projects span regenerated by the engine | required |
| `SCHEMA.md` | file | This file — apex of the repo's structure pyramid | required |
| `WIKI-TEST-REPORT.md` | file | Wiki.js integration test report | |
| `WIKIJS-QUICKSTART.md` | file | Wiki.js quickstart guide | |
| `docker-compose.yml` | file | Wiki.js + Postgres stack (mounts docs/ read-only) | |
| `mkdocs.yml` | file | MkDocs Material config for the published site | required |
| `repos.txt` | file | Aggregation input — regenerated from _data/projects.yml | generated |
| `requirements-docs.txt` | file | Python deps for the MkDocs build | |
| `requirements.txt` | file | Python deps for pipeline + engine | |

## Placement

- New fleet project → `_data/projects.yml`, then `python3 -m scripts.context_engine sync`
- New pipeline stage or utility → `scripts/`, registered in `scripts/SCHEMA.md` and `scripts/README.md`
- New engine build hook → `hooks.d/<stage>/NN-name` (executable), stages listed in `hooks.d/SCHEMA.md`
- New MCP tool → `mcp/server.py` + `mcp/README.md`
- New registry → `_data/`, registered in `_data/SCHEMA.md`

## Forbidden

- No hand edits to generated surfaces: `context/`, `docs/`, `repos.txt`,
  `site/`, or the README `AUTO:projects` span — fix the registry, the source
  repo, or the engine instead.
- No fleet-project source code here — it belongs in the project's own repo;
  this repo only carries distilled context about it.
